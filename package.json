{
  "name": "express-node-service",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal TypeScript and Express service scaffold",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\" eslint.config.mjs",
    "format": "prettier --write README.md package.json tsconfig.json tsconfig.check.json eslint.config.mjs .prettierrc.json .github/workflows/ci.yml src test",
    "format:check": "prettier --check README.md package.json tsconfig.json tsconfig.check.json eslint.config.mjs .prettierrc.json .github/workflows/ci.yml src test",
    "typecheck": "tsc -p tsconfig.check.json --noEmit",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.0.0",
    "helmet": "^8.3.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.0",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "supertest": "^7.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.70.0"
  }
}
