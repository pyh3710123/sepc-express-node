# Runtime mode: development, test, or production
NODE_ENV=development

# Network interface and port
HOST=0.0.0.0
PORT=3000

# Pino log level: fatal, error, warn, info, debug, trace, or silent
LOG_LEVEL=info

# Comma-separated HTTP(S) origins. Leave empty to disable browser cross-origin access.
CORS_ORIGINS=
